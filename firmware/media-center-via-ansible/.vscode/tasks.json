{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "0. install host prerequisites",
            "type": "shell",
            "command": "source \"${workspaceFolder}/.venv/bin/activate\" && sudo apt install -y ansible sshpass yamllint && ansible-galaxy collection install community.general",
            "problemMatcher": []
        },
        {
            "label": "1. apply without password",
            "type": "shell",
            "command": "source \"${workspaceFolder}/.venv/bin/activate\" && ansible-playbook ${file} -i hosts.yml",
            "problemMatcher": [],
            "group": "build"
        }
    ]
}